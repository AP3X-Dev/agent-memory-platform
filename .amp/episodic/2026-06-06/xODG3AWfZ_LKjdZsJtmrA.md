---
id: xODG3AWfZ_LKjdZsJtmrA
session_id: session-20260606-honcho-impl
agent_id: mcp
task: [project:amp] Implement docs/honcho-inspired-amp-enhancements.md — dialectic amp_ask, dream pass, inference_type facts, cards, tiered models
outcome: approved
created_at: "2026-06-06T09:11:55.637Z"
---

[project:amp] Implemented all Honcho-inspired enhancements on branch feat/honcho-enhancements. Shipped: (1) shared LLM client packages/core/src/llm.ts (OpenAiLlmClient/NullLlmClient, per-task models, transient retry) — the first chat-completion call site beyond extract.ts; (2) tiered models via AMPConfig.models + AMP_MODEL_* env, threaded through services-factory; (3) FactNode.inference_type deductive|inductive|abductive — added to types, fact.ts create Cypher + mapFactNode (default deductive), new fact_inference_type index, FACT_INFERENCE_MULTIPLIER in ranking (abductive 0.5), renderFactsMarkdown annotations [hypothesis]/[inferred]; consolidation mints inductive, real-time extraction mints deductive; (4) amp_ask — new Tier-1 retrieval tool: assembler.ask() runs ranked retrieval then LLM-synthesizes a cited answer, reasoning_level minimal/low/medium/high/max maps to retrieval+synthesis token budgets + model tier; (5) DreamEngine packages/core/src/dream.ts — gap-scan entities, mint low-confidence tentative abductive hypotheses, refreshCards (project_card from active DEDUCTIVE facts), never invalidates existing facts; exposed via `amp dream` CLI + nightly systemd timer + amp_consolidate action:'dream'; (6) KeyedSerialQueue per-entity mutex; (7) auto cards project_card/user_card in DEFAULT_BLOCKS, rendered after human blocks so they can't evict them under budget.

Verification: tsc build clean; full suite 1340 tests pass (0 fail) across 10 packages incl. live Neo4j. Ran an adversarial 13-agent review workflow (5 dimensions x verify) — fixed all 8 confirmed findings: corroborate-on-reinforcement must promote ONLY abductive (was wrongly catching inductive via status==='tentative'); added cross-process DistributedLock around DreamEngine.run via buildDreamEngine (covers both CLI and MCP dream paths, mutually exclusive with ConsolidationEngine on the same scope key); refreshCards now filters deductive-only; evolution timeline gained inference annotations; CLI runDream early-returns on no-LLM; parseHypotheses logs dropped malformed items.

Key gotchas learned: (a) the @amp/* workspace packages export TS SOURCE (exports.import -> ./src/index.ts) and run via `npx tsx`, NOT `node dist/cli.js` — dist holds only .d.ts; my systemd unit initially used node+dist and failed the smoke test, fixed to tsx. (b) CLAUDE.md and GEMINI.md are gitignored — the tracked doc surface is the .example files + README + skills + .cursorrules; updated tool counts 48->49 / 7->8 always-visible across all tracked surfaces. (c) vitest: `beforeEach(() => mock.mockReset())` arrow implicitly RETURNS the mock fn, which vitest calls as a teardown — if the mock throws, the test fails post-assertion; use a block body.