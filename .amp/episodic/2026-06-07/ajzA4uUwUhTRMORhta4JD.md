---
id: ajzA4uUwUhTRMORhta4JD
session_id: session-20260607-hermes-m4
agent_id: mcp
task: [project:ag3ntic-platform] M4 complete: Hermes employee runtime adapter over ACP built + tested
outcome: approved
created_at: "2026-06-07T16:37:54.251Z"
---

[project:ag3ntic-platform] Shipped platform_core/runtime_adapter package (M4). base.py = EmployeeRuntimeAdapter typing.Protocol (chat/start_run/stream_run_events/resume_after_approval/attach_capability/rotate_secret) + DTOs (RunStartRequest, RunEventDTO, PermissionRequest, ApprovalResolution) + registry (register_adapter/get_adapter/registered_kinds). acp.py = dependency-light ACP JSON-RPC 2.0 client over newline-delimited JSON on an async (reader,writer) byte stream; single read loop demuxes responses-by-id, session/update notifications (fed to PromptStream), and incoming session/request_permission requests (answered via installed PermissionHandler). hermes_adapter.py = HermesRuntimeAdapter: pluggable transports (subprocess spawns hermes-acp; docker_exec stubbed with clear HermesError; injected fake for tests). KEY DESIGN: each session owns ONE asyncio.Queue of RunEventDTO; a background pump task drains ACP updates AND the permission handler enqueues approval.requested/approval.resolved onto the same queue, so stream_run_events just drains until terminal. CRITICAL ORDERING FIX: must set runs.status=waiting_approval BEFORE enqueueing approval.requested, else a consumer reading status on event receipt races and sees 'running'. Permission handler blocks the ACP read loop awaiting a future; resume_after_approval resolves it with allow_once option id (approve) or deny option id (deny) — this is correct (agent is paused awaiting our reply, no deadlock). RunStore Protocol injected (SqlAlchemyRunStore default) keeps adapter DB-agnostic/testable. Map ACP/transport errors -> errors.HermesError (502). Wired orchestrator/__init__.py resolve_runtime_adapter('hermes'). Lean Dockerfile runtimes/hermes-employee FROM python:3.11-slim, hermes-agent[mcp,acp]+uv, ENTRYPOINT hermes-acp, USER 10001, no desktop tokens (grep clean). Tests: FakeAcpTransport plays the agent peer (reactive write()->ensure_future _on_frame); 5 adapter tests pass, full suite 68 pass, ruff clean.