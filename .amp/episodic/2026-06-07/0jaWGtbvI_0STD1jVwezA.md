---
id: 0jaWGtbvI_0STD1jVwezA
session_id: session-20260607-runtime-orchestrator
agent_id: mcp
task: [project:ag3ntic] M3: built the Runtime Orchestrator (§15) — the only Docker-touching service
outcome: approved
created_at: "2026-06-07T16:21:36.284Z"
---

[project:ag3ntic] Built M3 Runtime Orchestrator as platform_core/runtime_orchestrator/ (state.py, docker_client.py, orchestrator.py, service.py, router.py). It is the ONLY place that constructs a Docker client, via docker.DockerClient(base_url=settings.docker_socket_proxy_url) pointed at docker-socket-proxy (new config setting docker_socket_proxy_url default tcp://docker-socket-proxy:2375) — never docker.from_env(). docker_client exposes module-level async fns (pull_by_digest/ensure_network/ensure_volume/run_container/inspect_container/container_health/stop_container/remove_container/remove_network/remove_volume) each wrapping blocking docker-py 7.1.0 in asyncio.to_thread and mapping errors to errors.DockerError; tests monkeypatch this module entirely. state.py implements foundation §5.1 verbatim (RUNTIME_TRANSITIONS dict, can_transition, RESUMABLE/TERMINAL sets, async transition() that validates+updates status+writes a runtime_events row event_type runtime.<state>, raising ValidationError on illegal moves). KEY GOTCHA: runtime_events.ts uses server_default=func.now() so multiple transitions in one DB tx share a timestamp and ORDER BY ts is ambiguous → added a process-monotonic _monotonic_now() in state.transition that stamps ts explicitly (strictly increasing). orchestrator.provision_employee walks draft→provisioning→pulling_images→creating_network→creating_volumes→starting→healthy, applying the hardened HostConfig from the DELETED desktop docker_provider (cap_drop ALL, security_opt no-new-privileges, pids_limit/mem_limit/memswap=mem(swap off)/nano_cpus, user 10001:10001, read_only, tmpfs /tmp, ports={} NO host publish), per-workspace network ag3ntic_runtime_<workspace_id>, per-employee volume ag3ntic_employee_<employee_id>_hermes_home, container employee-<ws_slug>-<emp_slug>, full ag3ntic.* labels, image pulled BY DIGEST (image@sha256). Rolls employees.status up from runtime status via §5.3 table (healthy→running, stopped→paused, etc.). Idempotent: relaunch of a healthy employee returns existing row, no new container. stop_runtime/delete_runtime walk the machine (teardown container→volume→network). reconcile is an M3 hook (resume resumable states, recover unknown). router mounts POST .../employees/{id}/launch, GET .../runtime, POST .../stop under /api/v1 with authorize_workspace+CurrentKey. M3 simplification noted: Hermes/ACP wiring + lean image + secret copy-in + capability network + MCP sidecars + full reconcile loop + resource ledger are M4+; health gate is a docker-py inspect/running probe (stub for real /health). 5 tests pass (mock docker entirely), full suite 63 passed, ruff clean, app 36 routes.