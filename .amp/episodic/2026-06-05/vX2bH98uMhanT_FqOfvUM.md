---
id: vX2bH98uMhanT_FqOfvUM
session_id: session-20260605-032000
agent_id: mcp
task: [project:amp] Build human brain-dump ingestion + editable wiki round-trip so AMP is a human-curatable shared memory layer
outcome: approved
created_at: "2026-06-05T10:32:28.357Z"
---

[project:amp] Shipped the editable-wiki round-trip and human brain-dump feature. amp_compile now emits hidden per-claim anchors (`<!-- amp:sem-id -->`) on canonical domain-section claims (not the derived Key Decisions block). New WikiEditReconciler (packages/wiki/src/reconcile.ts) diffs an edited article vs the graph via those anchors: changed claim → supersede + re-link ABOUT + CORRECTS signal; new in-section prose → new human-authored Semantic; removed (only with the pre-edit original) → confidence penalty, never deleted. New amp_braindump tool ingests freeform text as durable human-authored memory (decay stable, conf 0.7, human-authored/source:brain-dump tags, ensure_project auto-creates the scope, entities CONTAINS-linked so they become articles) while keeping verbatim text as a Source; falls back to paragraph-claims since no ExtractionProvider is wired in bootstrap. Surfaces: viewer Edit button + POST /api/edit (driver injected into ViewerConfig, wired at CLI serve/build), amp_wiki_sync MCP tool, and `wiki sync` CLI. Tool count 42→44, docs + DOMAIN_TOOL_NAMES_MAP + drift-guard updated. Verified end-to-end against live Neo4j (brain dump→compile→edit→SUPERSEDES+CORRECTS edges, decay stable) and cleaned up. Two gotchas: Neo4j ints are BigInt (broke signal_count+1, fixed with Number()); untracked src/*.js build pollution silently shadowed .ts in vitest (deleted across wiki/mcp/neo4j; canonical build is dist/). Live mcp__amp__* tools won't expose the 2 new tools until the amp-mcp server is rebuilt+restarted.