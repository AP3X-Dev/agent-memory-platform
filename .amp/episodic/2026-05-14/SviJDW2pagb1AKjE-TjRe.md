---
id: SviJDW2pagb1AKjE-TjRe
session_id: session-20260514-optimizer55
agent_id: mcp
task: [project:oni-grid] optimization session 55: D14 — Codex managed-session resume/follow-up hardening
outcome: approved
created_at: "2026-05-14T13:18:53.272Z"
---

[project:oni-grid] Session 55 (commit 47b1539) hardened provider resume/follow-up semantics in agents/managed-agent-session.mjs. Root cause: Claude's ONI Core driver resumes a managed session natively (sessionId/sessionName carry conversation history), but Codex's exec driver has no resume flag in the installed API — a blocker_resolution continuation is a fresh process with zero in-memory history of earlier turns. The continuation prompt built by buildRunPrompt didn't compensate. Fix: added resumeModeForProvider(provider) → 'native' (claude) | 'fresh-exec' (codex), emitted in session.started and blocker.resolution_sent event data so the renderer can tell native-resume from cold-continuation. buildRunPrompt is now provider-aware: every blocker_resolution continuation carries the worktree path + managed session id, and fresh-exec providers additionally get explicit framing ("this is a fresh process… your prior work is already on disk in the worktree… inspect git status / existing files before continuing, do not restart from scratch"). Claude (native resume) is left without that framing since it already has the history. Convention/testing insight: the sidecar .mjs can't be unit-imported from a .ts test (vitest include is src/**/*.{test}.{ts,tsx}, tsc has no allowJs, and importing the .mjs pulls in @oni.bot/core). Solution: made buildRunPrompt observable through the EXISTING spawn-based dry-run replay harness — the dry-run path now emits runPrompt + resumeMode in the assistant.message data, so managedAgentSidecarReplay.test.ts asserts continuation framing without a live provider. resumeMode rides through normalizeManagedAgentEvent untouched (lives in event.data), no renderer change needed. With Sessions 52-55 (role-swarm merge coordination, real merge resolution, post-merge verification gate, provider resume hardening) the code-side D14 work is largely complete — what remains is live Claude/Codex blocker validation which needs an interactive desktop run with provider credentials, not runnable from the optimizer loop.