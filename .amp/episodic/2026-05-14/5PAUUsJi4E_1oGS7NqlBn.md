---
id: 5PAUUsJi4E_1oGS7NqlBn
session_id: session-20260514-workspace03
agent_id: mcp
task: [project:oni-grid] workspace optimizer session 3: add .oni/ spec contract parser/scaffold helpers
outcome: approved
created_at: "2026-05-14T17:42:32.428Z"
---

[project:oni-grid] Workspace loop session 3 (commit ebe75da on opt/oni-grid-workspace-architecture). Item #3: new src/lib/specContract.ts — pure helpers for the .oni/specs and .oni/changes layout, NO filesystem access (Tauri fs wiring is a later item; keeping helpers pure mirrors the hardening loop's pure-lib + thin-client convention). Contents: path builders (changeDir, changeArtifactPath, changeSpecDeltaPath, projectSpecPath); discoverChangeIds (distinct sorted ids from a path list, handles backslash paths); scaffoldChange (returns {changeDir, files[], specChange} descriptors — caller owns the write — with proposal/design/tasks templates + optional per-domain spec delta stubs, initial SpecChange in 'proposed' status); parseTasksMarkdown (lenient GitHub-checkbox parser: recognizes - [ ] / - [x] / * [X] / + [ ], silently skips headings, prose, malformed brackets, empty titles); parseScenariosFromSpec (### Scenario: <title> convention → unverified SpecScenario[]); parseChange (builds a SpecChange from a change-dir file map keyed by relative path, tolerates missing optional design.md, title from proposal's first heading falling back to changeId). Key convention decisions: (1) "archive path resolution" = projectSpecPath(domain) → .oni/specs/<domain>/spec.md — per the ADR, archiving an accepted change folds its spec deltas into the project-level spec, so that path IS the archive destination; no separate .oni/archive/ tree invented. (2) Parsers are deliberately lenient/total — a half-written tasks.md yields its valid tasks rather than throwing. (3) Scope discipline held: Task.specChangeId still not added, stays owned by item #4. Verification: TS 1714/1714 (+13), lint/tsc clean, cargo 163/163 + clippy clean (no Rust delta). Next: item #4 — spec approval + task extraction flow before dispatch.