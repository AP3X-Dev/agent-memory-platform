---
id: cgZAL_lLmNoS-UyHqqtfF
session_id: session-20260514-workspace12
agent_id: mcp
task: [project:oni-grid] workspace optimizer session 12: local host runner implementation
outcome: approved
created_at: "2026-05-14T19:46:42.880Z"
---

[project:oni-grid] Workspace loop session #12 (commit 0e8c788). Item #12: new src/lib/localHostRunner.ts — createLocalHostRunner() is the real Tauri-backed OniHostRunner, replacing createLocalHostRunnerSkeleton's no-ops. It's the canonical home for raw local host operations; UI/orchestration code obtains a runner and calls the contract, never the Tauri commands directly. Real wrappers: createWorkspace (create_worktree IPC + addWorkspace store action), deleteWorkspace (lookup workspace → remove_worktree IPC + removeWorkspace), writeTerminal/resizeTerminal (resolve a TerminalSession id → its ptyId via the store, then write_pty/resize_pty — sessionId is NOT a ptyId, the indirection matters), listPorts (reads the workspacePorts store slice — the scanner that populates it is item #20), getDiff (get_worktree_diff via the existing diffClient → DiffSummary; insertions/deletions are 0 because the Rust command reports files-changed + clean-state but not line counts). Honest placeholders, clearly marked: createTerminal/runSetup create TerminalSession records but no PTY spawn (items #15/#16 own the spawn binding), runAgent returns a placeholder AgentSession (agent-preset system is item #23). Key scope judgement: the optimizer's file list named useWorktree.ts but I left it untouched — runTaskInWorktree is a dispatch ORCHESTRATOR (preparePipeline → create_worktree → runTask → race handling), not a raw host op; routing it through the runner would be churn without behavioral gain and risks the existing tests. The optimizer file list is a hint, not gospel (consistent call across this loop — items #6, #9). The runner is the new raw-host-op layer; genuinely-thin invoke-wrapper hooks can delegate later if it ever pays off. 11 tests mock loadTauriInvoke + the store, assert command names + records. Verification: TS 1821/1821 (+11), lint/tsc clean, cargo 163/163 + clippy clean. Block 4 (host runner contract) is now functionally complete: contract (#10) + workspace-creation wiring (#11) + real local implementation (#12). Next: item #13 — convert Mission Board cards to spec/task/workspace hybrid cards (Block 5, UI).