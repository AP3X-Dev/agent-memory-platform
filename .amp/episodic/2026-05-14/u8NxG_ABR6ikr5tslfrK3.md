---
id: u8NxG_ABR6ikr5tslfrK3
session_id: session-20260514-workspace04
agent_id: mcp
task: [project:oni-grid] workspace optimizer session 4: spec approval + task extraction gate before dispatch
outcome: approved
created_at: "2026-05-14T17:59:04.117Z"
---

[project:oni-grid] Workspace loop session 4 (commit 78a817e). Item #4: the spec gate. Key insight: item #4's deliverable is LOGIC, not a UI rewrite — its four testable behaviors (creating a change yields tasks, approval enables dispatch, unapproved multi-agent dispatch blocked, explicit bypass works) are all pure-function/store contracts; a full Spec Change main-area view is a separate component item under the item #5 UI plan. New pure src/lib/specGate.ts: extractTasksFromChange (runs tasks.md through parseTasksMarkdown, emits Task[] with specChangeId + the change's scenario refs so reviewers verify against scenarios not chat) and evaluateDispatchGate (returns {allowed, reason} — single-manual always allowed; multi-agent/autonomous need a SpecChange in approved/implementing/verifying; explicit bypass flag for one-off fanout; archived/rejected/proposed all block). Type linkage finally landed: Task gained specChangeId + scenarioRefs (the reverse link deferred from sessions 2-3), Run gained specChangeId — both optional so no existing construction breaks. appStore specChanges slice + add/update/approve/rejectSpecChange; rejectSpecChange keeps the change + artifacts on record (ADR forbids silent deletion); left unbounded like tasks (operator-created, naturally bounded live state). Critical convention — "build alongside" wiring: the gate is wired into dispatchManagedRun but bypass is set to !run.specChangeId, so a run with no attached spec is the legacy/manual path and bypasses cleanly — all pre-existing managed-dispatch tests unchanged. The gate only bites once a specChangeId is attached and unapproved. Later items make spec-driven runs the default. Verification: TS 1732/1732 (+18: 10 specGate + 5 appStore.specChange + 3 dispatch gate), lint/tsc clean, cargo 163/163 + clippy clean. Next: item #6 — agent role allocation model + capacity (AgentRole/AgentLease/AgentPoolSnapshot), then #6A AMP memory-ref contract.