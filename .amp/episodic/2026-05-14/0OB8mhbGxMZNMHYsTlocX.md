---
id: 0OB8mhbGxMZNMHYsTlocX
session_id: session-20260514-workspace6A
agent_id: mcp
task: [project:oni-grid] workspace optimizer session 6A: AMP memory-ref contract for specs, workspaces, roles
outcome: approved
created_at: "2026-05-14T18:34:35.103Z"
---

[project:oni-grid] Workspace loop session #6A (commit be94995). Item #6A: the AMP memory-ref contract — closes discovered item #1 (premortem-guardrails gap). New types: MemoryRef, MemoryScope, MemoryScopeKind, MemoryUse, MemoryRefTarget. CORE PRINCIPLE pinned by code+test: a MemoryRef is a PROVENANCE POINTER, not state — it records which AMP entry (ampEntryId) informed a spec-change/task/workspace/agent-lease/review/merge and why (use + scopes + summary), and it deliberately has NO status field. The hard rule "AMP is durable alignment memory, not live state" is enforced structurally: current task/workspace/lease status always comes from the live SpecChange/Workspace/AgentLease/event state, never from a MemoryRef. A test asserts MemoryRef keys exclude 'status' and summarizeMemoryProvenance output has no status. MemoryUse is the store-rule taxonomy from the optimizer (accepted-lesson, rejected-assumption, stale-correction, review-outcome, qa-evidence, merge-outcome, rollback-rationale). MemoryScopeKind is the load-key dimension set (project/spec-change/task/workspace/role/file/outcome). New pure src/lib/memoryContext.ts: buildLoadKey sorts scopes deterministically (same scope set → same AMP key, cache-hit friendly); supersedeMemoryRef implements stale-memory correction — the old ref is KEPT (marked supersededByRefId), never deleted, so the correction trail survives; activeMemoryRefs filters superseded; summarizeMemoryProvenance returns counts-by-use + merged load key (no status). appStore: memoryRefs slice + attachMemoryRef + replaceMemoryRef (delegates to supersedeMemoryRef). A store test pins that attaching a ref creates no task and no spec change — memory binding never mutates live work. memoryRefs left unbounded for now (same call as tasks/specChanges/agentLeases). Verification: TS 1764/1764 (+14), lint/tsc clean, cargo 163/163 + clippy clean. Next: item #7 — Project/Host/Workspace/TerminalSession/WorkspacePort types (the workspace data model itself, Block 3).