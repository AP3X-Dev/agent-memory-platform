---
id: K5kVt06orGFKRT-AV7MPQ
session_id: session-20260514-workspace07
agent_id: mcp
task: [project:oni-grid] workspace optimizer session 7: workspace data model types
outcome: approved
created_at: "2026-05-14T18:42:19.012Z"
---

[project:oni-grid] Workspace loop session #7 (commit 5d2e145). Item #7 opens Block 3 (workspace source of truth). Added conservative type-only definitions to src/types/index.ts: Project, Host (+ HostKind = local/cerebro/e2b, HostStatus = online/offline/unknown), WorkspaceConfig (setup/run/teardown/ports — the shape .oni/config.json resolves to, parser is item #17), Workspace (+ WorkspaceStatus = creating/idle/running/blocked/review/merged/archived/failed), TerminalSession (+ TerminalSessionStatus = starting/running/exited/killed), WorkspacePort. Architecture: Workspace is the execution source of truth — an isolated worktree on a Host; spec-driven workspaces carry taskId/specChangeId/runId, a manual one-off may have none (both shapes test-covered). Convention pinned: TerminalSession and WorkspacePort are keyed by workspaceId (the optimizer's "include workspaceId on future session/port records" rule); the PTY lifecycle (ptyId, status) lives ON TerminalSession, deliberately separate from the optional paneId that views it — that separation is the foundation items #15/#16 build on so switching UI views never kills a session. Scope discipline: NO store wiring, NO behavior, existing PaneState/Task fields untouched — slices+selectors are item #8, config parsing is item #17. 10 construction tests. Verification: TS 1774/1774 (+10), lint/tsc clean, cargo 163/163 + clippy clean. Next: item #8 — workspace/host store slices + selectors (projects/hosts/workspaces/terminalSessions/workspacePorts arrays + CRUD + active-workspace-by-task/pane/run/branch selectors).