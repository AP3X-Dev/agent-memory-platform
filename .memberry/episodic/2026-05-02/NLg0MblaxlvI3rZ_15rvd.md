---
id: NLg0MblaxlvI3rZ_15rvd
session_id: session-20260502-phase3c7
agent_id: mcp
task: [project:fugazi] Phase 3c.7 complexity computation — McCabe cyclomatic, Sonar cognitive, Maintainability Index — implementation + tests
outcome: approved
created_at: "2026-05-02T12:04:11.751Z"
---

[project:fugazi] Phase 3c.7 (T077-test + T078) shipped on branch phase-3-foundation. Created packages/extract/src/complexity/{cyclomatic,cognitive,mi,index}.ts plus packages/extract/src/__tests__/complexity.test.ts. 25 tests pass (22 fixtures + 3 structural invariants). Full gate green (build/typecheck/lint/test/forbidden-strings/forbidden-fallow-env/verify-wasm). Key design choices: (1) Source-text scanning rather than AST walking for decision detection because the discriminated-union AST in ast/kinds.ts collapses ternaries, &&/||/??, and try/catch to UnknownExpression/UnknownStatement. Each metric file has its own self-contained scanner. (2) skipFunctionHeader() helper skips `function name(args)` so identifiers in the header don't get mis-counted (e.g., the function's own name preceding paren would otherwise trip recursion detection). (3) For cyclomatic, removed `do` from the keyword list — `do { } while (cond)` is counted by the `while` keyword, not double-counted. (4) For cognitive, added expectDoWhileTerminator flag so the trailing `while` of a do-while body is consumed without incrementing. (5) Halstead Volume uses simplified formula V=(n1+n2)*log2(n1+n2) where n1=distinct operators, n2=distinct operands; falls back to V=max(1, LOC*4) when scan yields zero. (6) MI rounded to 6 decimals via Number(x.toFixed(6)) for byte-equal serialisation. (7) Class methods emit baseline-complexity entries (cyclomatic=1, cognitive=0) with qualified `Class.method` names because parser doesn't expose method bodies. (8) Doc comments avoided literal "Math.random" string because the no-Math.random test reads source files; rewrote as "no non-deterministic primitives".</content>
<parameter name="tags">["project:fugazi", "extract", "complexity", "phase-3c"]