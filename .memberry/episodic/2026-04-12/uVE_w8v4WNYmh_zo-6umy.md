---
id: uVE_w8v4WNYmh_zo-6umy
session_id: autonomous-s05-permissions-2026-04-12
agent_id: mcp
task: [project:oni-code] Autonomous execution of S05 — Permissions Vertical
outcome: approved
created_at: "2026-04-12T21:44:23.712Z"
---

[project:oni-code] S05 complete on umbrella feat/coding-agent-parity-v0.2 (merge f08e2e4). Opens Phase 2 Safety & Policy. Ships PermissionManager closure factory with multi-source rule precedence (CLI → local → project → user → managed → builtin, first-match-wins), modes auto/interactive(default)/plan/deny/allow, swarm-aware EvaluationContext (agent_id/agent_role/parent_agent_id), inheritance-chain evaluation for subagents minus subagent_restricted_tools, 9 built-in sensitive patterns (.ssh/.env/.aws/.oni/*.key/etc.) extensible via sensitive_paths_extend, parseBashSafety (S04) consumed for bash sensitivity, sticky rules via permission_decide:always_allow persisting to .oni/permissions.json, runtime permission-prompt registry with fail-closed 60s timeout, tool-executor as single enforcement point, policy_blocked ToolResultClass (S02 reserved) now emitted, 5 RPC commands + 4 events + SessionStore permission_decision audit. New files: src/permissions/{manager,rules,sensitive,storage,index}.ts (5 files). Tests +58 (13 rules, 12 sensitive, 5 storage, 12 manager, 16 integration) → 716 pass + 1 skipped. Key architectural finding: Pregel buffers writer.emit() until node returns — promise-in-node prompt roundtrip would deadlock. Fix: new emitDirect callback bypasses buffering for permission events (permission_prompt/denied/granted/timeout); tool_start/end still via writer (post-permission). emitDirect now part of node-deps vocabulary, reusable for S06 hooks veto semantics, S10 plan mode pre-execute, S12 subagent notifications. Six approved deviations all architectural or consistent with PRP intent. Risks for S06+: settings hot-reload is per-evaluate snapshot; non-local rule buckets empty until future slices populate; tool_start fires before permission check (permission-aware UIs subscribe to permission_denied); subagent context synthesized in tests (real subagents S12 need zero changes); .oni/permissions.json single-process; bash safety parsed twice per call (<1ms acceptable); mode:"plan" is just rule-set effect, full workflow is S10. Phase 2: 1/3 done.