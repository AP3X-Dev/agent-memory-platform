---
id: EJLEKMSJBZQ5Y4WT3ZrRX
session_id: autonomous-s04-bash-hardening-2026-04-12
agent_id: mcp
task: [project:oni-code] Autonomous execution of S04 — Bash Tool Hardening
outcome: approved
created_at: "2026-04-12T21:07:01.637Z"
---

[project:oni-code] S04 complete on umbrella feat/coding-agent-parity-v0.2 (merge 39a5179). Completes Phase 1 of the parity roadmap. Ships BashController.exec cwd arg (unwinds S02 swap-restore anti-pattern), AST-level safety parser with fatal/destructive/suspicious severities and deny/warn/off modes (fatal always-blocks), background execution via run_in_background:true returning task_id immediately, BackgroundBashRegistry closure factory on runtime (register/get/list/kill/killByAgent/shutdown), live-tail bash_output_chunk events for foreground AND background, output temp-file spillover with rolling buffer tail, kill escalation (SIGTERM → kill_grace_ms → SIGKILL; Windows taskkill /T /F verified no wmic fallback needed), agent-scoped cleanup via runtime.abort() + runtime.destroy() with killByAgent. New files: src/tools/bash-safety.ts, src/tools/background-bash-registry.ts. RPC commands: bash_task_list/get/kill. Events: bash_task_started, bash_output_chunk, bash_task_finished, bash_safety_warning. Tests +42 (19 safety unit, 9 registry unit, 14 integration) → 658 pass + 1 skipped. Typecheck/lint/build clean, smoke PONG. 5 approved deviations: state_delta.last_background_task_id removed (schema limitation, task_id via output instead); foreground bash_output_chunk.stream="combined" (single-arg onUpdate; refactor in S17); redactTaskForRpc returns rolling_output_tail preview; runtime.destroy() as public awaited shutdown; sanitizeShellOutput re-exported. T14 cross-platform kill: taskkill /pid /T /F verified propagates to grandchildren via parent→child→grandchild harness; POSIX unchanged (process.kill(-pgid, sig) + detached:true). Phase 1 DONE: S01 loop discipline + agent role, S02 error taxonomy + rollback, S03 edit hardening, S04 bash hardening — all swarm-native, all green, zero regressions. Next: Phase 2 S05 Permissions — will emit the policy_blocked ToolResultClass reserved in S02.