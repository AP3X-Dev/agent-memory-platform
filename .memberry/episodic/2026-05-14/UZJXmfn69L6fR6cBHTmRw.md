---
id: UZJXmfn69L6fR6cBHTmRw
session_id: session-20260514-workspace08
agent_id: mcp
task: [project:oni-grid] workspace optimizer session 8: workspace/host store slices and selectors
outcome: approved
created_at: "2026-05-14T19:01:36.614Z"
---

[project:oni-grid] Workspace loop session #8 (commit 8a91f1c). Item #8: wired the Block-3 workspace data model into appStore. Five new slices — projects, hosts, workspaces, terminalSessions, workspacePorts — with plain CRUD: add/update/removeProject, add/update/removeHost, add/update/removeWorkspace, upsert/removeTerminalSession, setWorkspacePorts. Conventions: updateWorkspace bumps updatedAt (same as updateSpecChange); upsertTerminalSession keys by id (state update replaces in place, no dup); setWorkspacePorts does "port replacement by workspace" — drops only that workspace's prior ports and installs the freshly-observed set, other workspaces' ports untouched (isolation test-pinned). New pure src/lib/workspaceSelectors.ts keeps lookups out of the store: findWorkspaceByTask, findWorkspaceByBranch, findWorkspacesByRun (plural — role-swarm run owns several), findWorkspaceForPane (the pane→workspace link is INDIRECT: a pane views a TerminalSession which carries workspaceId), activeWorkspaces (filters merged/archived/failed). Note: the user refined the selectors mid-session to be active-workspace-aware (find* now also require isActiveWorkspace, findWorkspaceForPane ignores exited/killed sessions) — that refined version is what landed in 8a91f1c; suite green at TS 1791/1791. Pattern observed across this loop: the user does their own fix(opt-workspace) correction commits on top of session commits (e.g. 06afe0b for the RunModal manualBypass toggle / discovered item #4) — concurrent refinement is normal here. cargo 163/163 + clippy clean (no Rust changes). Next: item #9 — deriveWorkspaceStatus pure helper (maps panes + managed sessions + blockers + ports → WorkspaceStatus).