---
id: eLC_r7k0TLyQegLsr6uJD
session_id: session-20260514-workspace11
agent_id: mcp
task: [project:oni-grid] workspace optimizer session 11: wrap local worktree dispatch as workspace creation
outcome: approved
created_at: "2026-05-14T19:35:29.161Z"
---

[project:oni-grid] Workspace loop session #11 (commit 08bbe97). Item #11: runTaskInWorktree (src/hooks/useWorktree.ts) now creates a durable Workspace record on successful local dispatch — Workspace is the execution source of truth, the pane is just a view onto it. Built ALONGSIDE the legacy path: the existing pane-state flow (runTask mutating pane state + the respawn effect) is untouched. KEY design — race safety is STRUCTURAL not defensive: workspace creation happens strictly AFTER the paneId===null dispatch-race check. The race path removes the orphaned worktree and throws BEFORE any Workspace is constructed, so a failed dispatch can never leak a Workspace record — no cleanup code needed, just ordering. A test pins workspaces stays empty on the race; another asserts the workspace is created on success. Field sourcing: hostId 'local' (a LOCAL_HOST_ID const — remote hosts get their own ids behind the same OniHostRunner contract), taskId from the task, specChangeId carried from the task (spec-driven tasks keep their link into the workspace), runId from store.activeRun?.id, branch/worktreePath from pipeline+worktree, projectId = projectPath (no Project record registered by this path; richer Project is a later concern). RunTaskInWorktreeResult gained workspaceId (additive — existing callers reading paneId/worktree unaffected); task.dispatched events now carry workspaceId. Mode-B discovery → new backlog item #6 (High): dispatchManagedRun (the managed role-swarm dispatch path) creates worktrees per lane but NO Workspace records — managed-run lanes are invisible to the workspace data model (no Workspace, no derived status, no workspace-keyed diff/merge). It should create a Workspace per lane the same way runTaskInWorktree now does. Verification: TS 1810/1810 (2 existing useWorktree tests gained assertions, no new it() blocks), lint/tsc clean, cargo 163/163 + clippy clean. Next: item #12 — the real local host runner implementation (replace createLocalHostRunnerSkeleton's no-ops with Tauri-backed calls).