---
id: 8-Q5cLE65ShLKc_wFZVnA
session_id: session-20260514-workspace09
agent_id: mcp
task: [project:oni-grid] workspace optimizer session 9: deriveWorkspaceStatus pure helper
outcome: approved
created_at: "2026-05-14T19:13:00.127Z"
---

[project:oni-grid] Workspace loop session #9 (commit 4064ee1). Item #9: deriveWorkspaceStatus(workspace, panes, agentSessions, blockers, ports) — a pure helper that derives a workspace's live status. CORE PRINCIPLE: workspace status is DERIVED from live signals, never trusted blindly from a stale workspace.status, never read from AMP. The helper scopes the full live arrays to the workspace itself: a session belongs by matching branch / cwd / taskId; a pane by worktreePath / branch; a blocker by sessionId-in-workspace or taskId; a port by workspaceId. Precedence (highest first): sticky terminal (merged/archived — live signals can NEVER drag them back) → blocked (any open blocker) → failed (a failed managed session OR an errored pane) → running (a live non-terminal session, an actively-working pane [running/thinking/testing/waiting], or an open dev-server port) → review (all sessions finished + ≥1 done — the "done-with-diff" case) → creating → idle. Interpretation note: the signature has `ports` but no diff param, so "done-with-diff → review" = "all sessions finished and at least one done" (a completed managed session implies reviewable work). Scope decision (reusable judgement): the optimizer's file list named managedAgentSession.ts, but workspace-status logic doesn't belong in the managed-session module — put deriveWorkspaceStatus in workspaceSelectors.ts (the workspace pure-helper home) and imported isTerminalAgentSessionState from managedAgentSession. The optimizer file list is a HINT not gospel — same call as item #6 (AgentRole already existed). 11 tests incl. a signal-scoping test (another workspace's pane/session is ignored). Verification: TS 1802/1802, lint/tsc clean, cargo 163/163 + clippy clean. Next: item #10 — OniHostRunner interface + local runner skeleton (type-only contract: createWorkspace/deleteWorkspace/runSetup/runAgent/createTerminal/writeTerminal/resizeTerminal/listPorts/getDiff).