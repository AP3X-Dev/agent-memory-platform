---
id: jBQ_KgdAjMfaDHUp6Jla0
session_id: session-20260514-workspace4A
agent_id: mcp
task: [project:oni-grid] workspace optimizer session 4A: close the no-spec managed dispatch bypass
outcome: approved
created_at: "2026-05-14T18:25:01.571Z"
---

[project:oni-grid] Workspace loop session #4A (commit 7f6c3d3). Closed a flaw in Session 4's own work: Session 4 wired the spec gate into dispatchManagedRun with `bypass: !run.specChangeId` — meaning the ABSENCE of a spec was itself a silent bypass, so every legacy managed run skipped the architecture rule by default. That defeated the gate. Fix: Run gained `manualBypass?: boolean` — explicit operator one-off intent. dispatchManagedRun now passes `bypass: run.manualBypass === true`. A managed run with neither an approved SpecChange nor manualBypass is now BLOCKED — the architecture rule holds by default and bypass is opt-in intent, not the fall-through. RunModal sets manualBypass: true on runs it creates (a START RUN click IS explicit operator dispatch intent; a future SpecChange-selection surface will let it attach an approved specChangeId instead — that's the real end state). Test strategy that's reusable: useManagedRunDispatch.test.ts's makeRun() default carries manualBypass: true (those are dispatch-MECHANICS tests = explicit operator runs); the two spec-gate tests override manualBypass: false so the gate actually evaluates the SpecChange (otherwise bypass:true short-circuits before the status check). Mode-B discovery → new backlog item #3: the run-rehydration path (useActiveRunPersistence → runClient → runs.rs) does NOT persist manualBypass/specChangeId, so an active+autoDispatch run rehydrated after app restart reaches dispatchManagedRun with neither field and is conservatively BLOCKED. That's a safe default (no spontaneous re-fanout on restart) but it should be a deliberate decision — logged Medium, needs runs.rs schema + runClient mapping. Convention reinforced: a "build alongside" temporary bypass must be an explicit opt-in field, never an absence-of-data default — absence-of-data defaults silently defeat the rule they're meant to introduce. Verification: TS 1750/1750, lint/tsc clean, cargo 163/163 + clippy clean. Next: #6A AMP memory-ref contract, then #7 workspace architecture types.